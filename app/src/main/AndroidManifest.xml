<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.untibunti">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".VideoActivity"></activity>
        <activity android:name=".ShopDisplayActivity" />
        <activity android:name=".CategoryWiseShopDisplay" />
        <activity android:name=".CouponRegisterActivity" />
        <activity android:name=".ShopRegisterActivity" />
        <activity android:name=".VerificationActivity" />
        <activity android:name=".PhoneAuthActivity" />
        <activity android:name=".About" />
        <activity android:name=".TaskActivity" />
        <activity android:name=".Home" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>